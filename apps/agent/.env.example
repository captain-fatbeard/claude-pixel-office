# Required: URL of the web app's webhook endpoint
WEBHOOK_URL=http://localhost:3000/api/webhook

# Machine identifier (defaults to hostname)
MACHINE_ID=my-macbook
MACHINE_NAME=My MacBook

# Optional: GitHub username for weekly contribution chart
GITHUB_USERNAME=

# Poll interval in ms (default: 2000)
POLL_INTERVAL=2000
